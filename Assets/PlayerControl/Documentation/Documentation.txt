Documentation coming shortly... as available free time may allow.

ok, let us first put the setup steps for adding the example playercontrol controller to each single vehicle controller in own scene that has then opted to add vehicle entry script in such scene as such would be needed to test and is the entire point of including an example playercontrol script for such purposes in the first place. Ok, then let us add additional steps for same type of playercontrol addition but to the all vehicles scene setup instructions that has obviously the optional entry script addition as such again would be needed...
